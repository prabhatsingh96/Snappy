package app.android.snappay.util

object FirebaseUtils {

    private val TAG = FirebaseUtils::class.simpleName

//    fun logFirebaseToken() = Log.i(TAG, "logFirebaseToken: ${FirebaseInstanceId.getInstance().token}")
}
